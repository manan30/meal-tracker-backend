const USER_EXISTS = 'User already exists';

const SERVER_ERROR = (err: Error) => `Internal server error: ${err}`;

const ACCOUNT_CREATED = 'Account created successfully';

export { USER_EXISTS, SERVER_ERROR, ACCOUNT_CREATED };
